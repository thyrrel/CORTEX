name: C.O.R.T.E.X. Core Execution

# 1. Gatilho (Trigger)
on:
  push:
    branches:
      - main
  workflow_dispatch:

# 2. Definição dos Jobs
jobs:
  run_cortex_core:
    # 2.1. Configuração do Ambiente
    runs-on: ubuntu-latest

    # 2.2. Mapeamento de Variáveis de Ambiente (Secrets)
    env:
      CORTEX_DB_HOST: ${{ secrets.CORTEX_DB_HOST }}
      CORTEX_DB_USER: ${{ secrets.CORTEX_DB_USER }}
      CORTEX_DB_PASS: ${{ secrets.CORTEX_DB_PASS }}
      CORTEX_DB_NAME: ${{ secrets.CORTEX_DB_NAME }}
    
    # 2.3. Sequência de Passos (Steps)
    steps:
      # Passo 1: Checkout do Repositório
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      # Passo 2: Configuração do Python
      - name: 🐍 Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      # Passo 3: Instalação de Dependências
      - name: 📦 Install MySQL Connector
        run: |
          python -m pip install --upgrade pip
          pip install mysql-connector-python

      # --- Bloco de Validação (Teste) ---
      - name: 🔍 Validate DB Connection Logic
        run: |
          echo "Simulando teste de conexão/persistência..."
          python -c "import os; print('DB Host:', os.environ.get('CORTEX_DB_HOST'))"

      # --- Bloco de Execução Principal (MODIFICADO para execução direta) ---
      - name: ▶️ Run C.O.R.T.E.X. Core Execution
        run: |
          # Define o PYTHONPATH para a raiz do projeto. 
          # Isso é CRUCIAL para que a importação relativa funcione dentro do __main__.py.
          export PYTHONPATH=$PWD:$PYTHONPATH
          
          # Executa o script principal diretamente via caminho de arquivo.
          # Isso contorna o erro de 'ModuleNotFoundError' que o -m estava gerando.
          python backend/core/__main__.py
