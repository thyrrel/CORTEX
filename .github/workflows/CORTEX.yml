name: C.O.R.T.E.X. Core Execution

# 1. Gatilho (Trigger)
on:
  push:
    branches:
      - main
  workflow_dispatch:

# 2. Definição dos Jobs
jobs:
  run_cortex_core:
    # 2.1. Configuração do Ambiente
    runs-on: ubuntu-latest

    # 2.2. Mapeamento de Variáveis de Ambiente (Secrets)
    env:
      CORTEX_DB_HOST: ${{ secrets.CORTEX_DB_HOST }}
      CORTEX_DB_USER: ${{ secrets.CORTEX_DB_USER }}
      CORTEX_DB_PASS: ${{ secrets.CORTEX_DB_PASS }}
      CORTEX_DB_NAME: ${{ secrets.CORTEX_DB_NAME }}
    
    # 2.3. Sequência de Passos (Steps)
    steps:
      # Passo 1: Checkout do Repositório
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      # Passo 2: Configuração do Python
      - name: 🐍 Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          
      # Passo 3: Instalação de Dependências
      - name: 📦 Install MySQL Connector
        run: |
          python -m pip install --upgrade pip
          pip install mysql-connector-python

      # --- Bloco de Execução Unificado (Resolve Ambos os Erros) ---
      - name: ▶️ Execute CORTEX Core and Validate Env
        run: |
          # 1. Teste de Variáveis (Acessando diretamente no shell para debug)
          echo "Simulando teste de conexão/persistência..."
          echo "Verificação CORTEX_DB_HOST: $CORTEX_DB_HOST"
          
          # 2. Execução de Módulo para resolver importações relativas
          # O Python procura o pacote 'backend.core' e executa seu __main__.py.
          # Nota: O erro de 'DB Host:' vazio anteriormente está provavelmente mascarado 
          # pelo erro fatal de importação. Se este passo funcionar, 
          # a próxima falha será de credenciais/conexão.
          python -m backend.core
